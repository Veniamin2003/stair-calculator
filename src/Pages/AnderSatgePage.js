import React from 'react';
import {Layout} from "../Layout";
import AnderStageContainer from "../Components/AnderStage/AnderStageContainer";

const AnderStagePage = () => {
    return (
        <Layout>
            <AnderStageContainer />
        </Layout>
    );
};

export default AnderStagePage;